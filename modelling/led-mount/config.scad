$fn = 128;

switch_width = 14.5;
switch_height = 8;

frame_thickness = 5;
frame_depth = 6;
frame_width = switch_width + frame_thickness * 2;
frame_height = switch_height + frame_thickness * 2;

bolt_hole_radius = 2.05;

clip_hole_radius = (frame_thickness - 1) / 2;
clip_rod_radius = (frame_thickness - 1.5) / 2;
