$fn = 128;

switch_width = 14.5;
switch_height = 8;

frame_thickness = 3;
frame_depth = 6;
frame_width = switch_width + frame_thickness * 2;
frame_height = switch_height + frame_thickness * 2;

bolt_hole_radius = 1.5;
